const CONFIG = { 
    MIGRATE: { // define the migration settings (what you want to migrate)
        COUPONS: false,
        PRODUCTS: false,
        PRICES: true,
    }
}

module.exports = { CONFIG };
